<Window x:Class="RestaurantCarol.Views.AdreseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adresele mele" 
        Height="650" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="#F4B860"/>
            <Setter Property="Foreground" Value="#3A3A3A"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="{TemplateBinding Background}"/>
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0A050"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#F4B860"/>
            <Setter Property="Foreground" Value="#3A3A3A"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,6,12,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="14"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0A050"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#D85A4A"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C04830"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" 
            CornerRadius="20"
            Margin="20">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                              BlurRadius="20" 
                              ShadowDepth="0" 
                              Opacity="0.3"/>
        </Border.Effect>

        <Grid Margin="30,20,30,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Adresele mele"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="#1A1A1A"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"/>
                <Button Style="{StaticResource CloseButton}"
                        Content="✕"
                        HorizontalAlignment="Right"
                        Command="{Binding InchideCommand}"/>
            </Grid>

            <ScrollViewer Grid.Row="1" 
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <StackPanel>

                    <TextBlock Text="Nu ai nicio adresa salvata. Adauga prima ta adresa folosind butonul de mai jos."
                               FontSize="14"
                               FontStyle="Italic"
                               Foreground="#888888"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               Margin="0,40,0,0"
                               Visibility="{Binding VizibilitateMesajGol}"/>

                    <ItemsControl ItemsSource="{Binding Adrese}"
                                  Visibility="{Binding VizibilitateLista}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#FDF1DC"
                                        CornerRadius="12"
                                        Padding="15,12,15,12"
                                        Margin="0,5,0,5"
                                        BorderBrush="#F4B860"
                                        BorderThickness="1">
                                    <StackPanel>

                                        <Grid Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0"
                                                       Text="{Binding AdresaText}" 
                                                       FontSize="15"
                                                       FontWeight="SemiBold"
                                                       Foreground="#1A1A1A"
                                                       TextWrapping="Wrap"
                                                       VerticalAlignment="Center"/>

                                            <Border Grid.Column="1"
                                                    Background="#7A6B14"
                                                    CornerRadius="10"
                                                    Padding="8,3,8,3"
                                                    VerticalAlignment="Center"
                                                    Margin="10,0,0,0"
                                                    Visibility="{Binding EsteImplicita, Converter={StaticResource BoolToVisibility}}">
                                                <TextBlock Text="IMPLICITA"
                                                           FontSize="10"
                                                           FontWeight="Bold"
                                                           Foreground="White"/>
                                            </Border>
                                        </Grid>

                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Seteaza ca implicita"
                                                    Style="{StaticResource ActionButton}"
                                                    Command="{Binding DataContext.SetImplicitaCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    Visibility="{Binding EsteImplicita, Converter={StaticResource BoolToInvisibility}}"/>

                                            <Button Content="Editeaza"
                                                    Style="{StaticResource ActionButton}"
                                                    Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"/>

                                            <Button Content="Sterge"
                                                    Style="{StaticResource DeleteButton}"
                                                    Command="{Binding DataContext.StergeCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"/>
                                        </StackPanel>

                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>

            <Button Grid.Row="2"
                    Content="+ Adauga adresa noua"
                    Background="#F4B860"
                    Foreground="#3A3A3A"
                    FontSize="15"
                    FontWeight="Bold"
                    Height="48"
                    Cursor="Hand"
                    BorderThickness="0"
                    Margin="0,15,0,0"
                    Command="{Binding AdaugaCommand}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="24">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0A050"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

        </Grid>
    </Border>
</Window>