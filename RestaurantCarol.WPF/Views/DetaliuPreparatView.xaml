<Window x:Class="RestaurantCarol.Views.DetaliuPreparatView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detalii Preparat" 
        Height="750" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.Resources>
        <!-- Stil pentru butonul de Close (X) -->
        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="#F4B860"/>
            <Setter Property="Foreground" Value="#3A3A3A"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="{TemplateBinding Background}"/>
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0A050"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Stil pentru butoanele cantitate (+ si -) -->
        <Style x:Key="QuantityButton" TargetType="Button">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="#F4B860"/>
            <Setter Property="Foreground" Value="#3A3A3A"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="{TemplateBinding Background}"/>
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0A050"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Stil pentru badge-uri de alergeni - in tonurile aplicatiei -->
        <Style x:Key="AlergenBadge" TargetType="Border">
            <Setter Property="Background" Value="#FDF1DC"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="10,5,10,5"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="BorderBrush" Value="#F4B860"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- ScrollBar custom - subtila si elegant -->
        <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#C0B580" 
                                CornerRadius="4"
                                Margin="2,0,2,0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{TemplateBinding Background}">
                            <Track Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbStyle}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand"
                                                  Opacity="0"/>
                                </Track.IncreaseRepeatButton>
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand"
                                                  Opacity="0"/>
                                </Track.DecreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0"/>
                            <ScrollBar Grid.Column="1" 
                                       Name="PART_VerticalScrollBar"
                                       Style="{StaticResource CustomScrollBar}"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Container principal cu colturi rotunjite si umbra -->
    <Border Background="White" 
            CornerRadius="20"
            Margin="20">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                              BlurRadius="20" 
                              ShadowDepth="0" 
                              Opacity="0.3"/>
        </Border.Effect>

        <Grid Margin="30,20,30,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Butonul X -->
                <RowDefinition Height="*"/>
                <!-- Continut scrolabil -->
                <RowDefinition Height="Auto"/>
                <!-- Buton Adauga + Cantitate -->
            </Grid.RowDefinitions>

            <!-- Butonul X dreapta sus -->
            <Button Grid.Row="0"
                    Style="{StaticResource CloseButton}"
                    Content="✕"
                    HorizontalAlignment="Right"
                    Click="Inchide_Click"/>

            <!-- Continut scrolabil -->
            <ScrollViewer Grid.Row="1" 
                          Style="{StaticResource CustomScrollViewer}"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Margin="0,5,0,15">
                <StackPanel>

                    <!-- Poza preparat - acum ROTUNDA -->
                    <Border Width="280" Height="280" 
                            CornerRadius="140"
                            ClipToBounds="True"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,20">
                        <Border.Background>
                            <ImageBrush x:Name="pozaPreparat" 
                                        Stretch="UniformToFill"/>
                        </Border.Background>
                    </Border>

                    <!-- Denumire -->
                    <TextBlock x:Name="denumireText"
                               Text="Denumire preparat"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="#1A1A1A"
                               HorizontalAlignment="Left"/>

                    <!-- Pret -->
                    <TextBlock x:Name="pretText"
                               Text="0.00 RON"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="#1A1A1A"
                               Margin="0,5,0,2"/>

                    <!-- Cantitate portie - sub pret, fara "portie" -->
                    <TextBlock x:Name="cantitatePortieText"
                               Text="0g"
                               FontSize="16"
                               Foreground="#666666"
                               Margin="0,0,0,10"/>

                    <!-- Separator -->
                    <Border Height="1" 
                            Background="#E0E0E0" 
                            Margin="0,10,0,15"/>

                    <!-- Sectiune Ingrediente -->
                    <StackPanel x:Name="ingredienteSection" Margin="0,0,0,15">
                        <TextBlock Text="Ingrediente"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="#1A1A1A"
                                   Margin="0,0,0,8"/>
                        <TextBlock x:Name="descriereText"
                                   Text="Descriere preparat..."
                                   FontSize="14"
                                   Foreground="#444444"
                                   TextWrapping="Wrap"
                                   LineHeight="20"/>
                    </StackPanel>

                    <!-- Sectiune Alergeni -->
                    <StackPanel x:Name="alergeniSection" Margin="0,0,0,15">
                        <TextBlock Text="Alergeni"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="#1A1A1A"
                                   Margin="0,0,0,8"/>
                        <ItemsControl x:Name="alergeniList">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource AlergenBadge}">
                                        <TextBlock Text="{Binding Denumire}" 
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   Foreground="#7A6B14"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!-- Sectiune Valori Nutritionale -->
                    <StackPanel x:Name="valoriSection" Margin="0,0,0,15">
                        <TextBlock Text="Valori nutriționale"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="#1A1A1A"
                                   Margin="0,0,0,8"/>
                        <StackPanel x:Name="valoriList"/>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>

            <!-- ZONA JOS: Cantitate + Buton Adauga -->
            <StackPanel Grid.Row="2" 
                        x:Name="cosSection"
                        Margin="0,10,0,0">

                <!-- Selector cantitate -->
                <Border Background="#FDF1DC"
                        CornerRadius="20"
                        Padding="8,4,8,4"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource QuantityButton}"
                                Content="−"
                                Click="Minus_Click"/>
                        <TextBlock x:Name="cantitateText"
                                   Text="1"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="#1A1A1A"
                                   VerticalAlignment="Center"
                                   Margin="20,0,20,0"/>
                        <Button Style="{StaticResource QuantityButton}"
                                Content="+"
                                Click="Plus_Click"/>
                    </StackPanel>
                </Border>

                <!-- Buton Adauga - acum portocaliu, ca paleta aplicatiei -->
                <Button x:Name="adaugaButton"
                        Background="#F4B860"
                        Foreground="#3A3A3A"
                        FontSize="16"
                        FontWeight="Bold"
                        Height="55"
                        Cursor="Hand"
                        BorderThickness="0"
                        Click="Adauga_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="30">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E0A050"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <TextBlock x:Name="adaugaText" Text="Adaugă în coș - 0.00 RON"/>
                </Button>

                <!-- Mesaj pentru oaspete -->
                <Border x:Name="oaspeteSection"
                        Visibility="Collapsed"
                        Background="#FDF1DC"
                        CornerRadius="12"
                        Padding="20,15,20,15"
                        BorderBrush="#F4B860"
                        BorderThickness="1">
                    <TextBlock Text="Logheaza-te pentru a comanda"
                               FontSize="14"
                               FontStyle="Italic"
                               Foreground="#7A6B14"
                               TextAlignment="Center"
                               HorizontalAlignment="Center"/>
                </Border>
            </StackPanel>

        </Grid>
    </Border>
</Window>