<Window x:Class="RestaurantCarol.Views.StareComandaListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Comenzile mele"
        Height="520" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Border Background="White" CornerRadius="12" Padding="24" Margin="12">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.25"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <TextBlock Text="Comenzile tale" FontSize="22" FontWeight="Bold" Foreground="#1B7A6B"/>
                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top"
                        Width="36" Height="36" Background="#F4B860" Foreground="#3A3A3A"
                        FontWeight="Bold" BorderThickness="0" Cursor="Hand" 
                        Command="{Binding InchideCommand}"/>
            </Grid>

            <TextBlock Grid.Row="1" Margin="0,12,0,16" FontSize="13" Foreground="#666666"
                       Text="Apasa pe o comanda pentru a vedea starea si a o anula (daca e activa)."/>

            <TextBlock Grid.Row="2"
                       Text="Nu ai comenzi plasate."
                       FontSize="15" Foreground="#888888"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="{Binding VizibilitateEmpty}"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ListBox ItemsSource="{Binding Comenzi}"
                         SelectedItem="{Binding Selectata, Mode=TwoWay}"
                         BorderThickness="0" Background="Transparent">
                    <ListBox.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" 
                                      Command="{Binding DeschideSelectataCommand}"/>
                    </ListBox.InputBindings>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,0,0,8"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="bd" Background="#F5F5F5" CornerRadius="8" Padding="14"
                                                BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="bd" Property="Background" Value="#E8F5F2"/>
                                                <Setter TargetName="bd" Property="BorderBrush" Value="#1B7A6B"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" FontSize="15" FontWeight="Bold" Foreground="#333333"
                                               Text="{Binding CodComanda, StringFormat=Comanda #{0}}"/>
                                    <TextBlock Grid.Column="1" FontSize="12" Foreground="#888888"
                                               Text="{Binding DataComanda, StringFormat=dd.MM.yyyy HH:mm}"/>
                                </Grid>
                                <TextBlock Margin="0,6,0,0" FontSize="13" Foreground="#1B7A6B" FontWeight="SemiBold"
                                           Text="{Binding StareAfisata}"/>
                                <TextBlock FontSize="12" Foreground="#666666" Margin="0,2,0,0"
                                           Text="{Binding CostTotal, StringFormat=Total: {0:F2} RON}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>

            <Button Grid.Row="3" Content="Deschide comanda selectata" Margin="0,16,0,0"
                    Height="40" Background="#1B7A6B" Foreground="White" FontWeight="SemiBold"
                    BorderThickness="0" Cursor="Hand" 
                    Command="{Binding DeschideSelectataCommand}"/>
        </Grid>
    </Border>
</Window>