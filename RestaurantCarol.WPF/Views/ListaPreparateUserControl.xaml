<UserControl x:Class="RestaurantCarol.Views.ListaPreparateUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="PreparatCircle" TargetType="Button">
            <Setter Property="Width" Value="280"/>
            <Setter Property="Height" Value="280"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse x:Name="ellipse"
                                     Stroke="#373334" 
                                     StrokeThickness="3"/>
                            <ContentPresenter HorizontalAlignment="Stretch" 
                                              VerticalAlignment="Stretch"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ellipse" Property="Opacity" Value="0.8"/>
                                <Setter TargetName="ellipse" Property="StrokeThickness" Value="5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BackButton" TargetType="Button">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#D2AF6D"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#373334"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="{TemplateBinding Background}" 
                                     Stroke="#373334" 
                                     StrokeThickness="2"/>
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,30">

                <Button Style="{StaticResource BackButton}"
                        Content="←"
                        Click="Back_Click"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"/>

                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Center">

                    <Border x:Name="logoBorder"
                            Width="220" Height="220"
                            CornerRadius="110"
                            HorizontalAlignment="Center"
                            ClipToBounds="True">
                        <Border.Background>
                            <ImageBrush x:Name="logoImage" 
                                        Stretch="UniformToFill"/>
                        </Border.Background>
                    </Border>

                    <TextBlock x:Name="titluText"
                               Text="Categorie"
                               TextAlignment="Center"
                               FontSize="28"
                               FontWeight="SemiBold"
                               Foreground="White"
                               Margin="0,15,0,0"
                               HorizontalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="8" 
                                              ShadowDepth="2" Opacity="0.8"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>
            </Grid>

            <ItemsControl Grid.Row="1"
                          x:Name="preparateList"
                          ItemsSource="{Binding}"
                          Margin="50,0,50,40">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel HorizontalAlignment="Center" 
                                    VerticalAlignment="Bottom"
                                    Margin="20,20,20,40">
                            <Button Style="{StaticResource PreparatCircle}"
                                    Tag="{Binding}"
                                    Click="Preparat_Click">
                                <Border Width="280" Height="280" 
                                        CornerRadius="140"
                                        ClipToBounds="True">
                                    <Border.Background>
                                        <ImageBrush ImageSource="{Binding PrimaCalePoza, FallbackValue='/Images/carol_logo.png', TargetNullValue='/Images/carol_logo.png'}" 
                                                    Stretch="UniformToFill"/>
                                    </Border.Background>
                                </Border>
                            </Button>
                            <TextBlock Text="{Binding Denumire}" 
                                       TextAlignment="Center"
                                       FontSize="22"
                                       FontWeight="SemiBold"
                                       Foreground="White"
                                       Margin="0,15,0,0"
                                       HorizontalAlignment="Center"
                                       TextWrapping="Wrap"
                                       MaxWidth="280">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="8" 
                                                      ShadowDepth="2" Opacity="0.8"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>
</UserControl>