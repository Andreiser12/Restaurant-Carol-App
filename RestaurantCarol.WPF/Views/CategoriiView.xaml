<Window x:Class="RestaurantCarol.Views.CategoriiView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:RestaurantCarol.ViewModels"
        xmlns:convert="clr-namespace:RestaurantCarol.Converters"
        Title="Gestionare Categorii" Height="600" Width="500">

    <Window.DataContext>
        <vm:CategorieViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <convert:CategorieConvert x:Key="categorieConvert"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Denumire:" Width="80" VerticalAlignment="Center"/>
            <TextBox Name="txtDenumire" Width="300" Height="30" 
                     Text="{Binding ElementName=categoriiGrid, 
                                    Path=SelectedItem.Denumire, 
                                    UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Adauga" Width="100" Height="30" Margin="0,0,10,0"
                    Command="{Binding AddCommand}">
                <Button.CommandParameter>
                    <MultiBinding Converter="{StaticResource categorieConvert}">
                        <Binding ElementName="txtDenumire" Path="Text"/>
                    </MultiBinding>
                </Button.CommandParameter>
            </Button>

            <Button Content="Modifica" Width="100" Height="30" Margin="0,0,10,0"
                    Command="{Binding ModifyCommand}"
                    CommandParameter="{Binding ElementName=categoriiGrid, Path=SelectedItem}"/>

            <Button Content="Sterge" Width="100" Height="30"
                    Command="{Binding DeleteCommand}"
                    CommandParameter="{Binding ElementName=categoriiGrid, Path=SelectedItem}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" Name="categoriiGrid" 
                  ItemsSource="{Binding CategoriiList}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Denumire" 
                                    Binding="{Binding Denumire}" 
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>