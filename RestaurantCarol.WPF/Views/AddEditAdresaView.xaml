<Window x:Class="RestaurantCarol.Views.AddEditAdresaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adresa" 
        Height="380" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Border Background="White" 
            CornerRadius="20"
            Margin="20">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                              BlurRadius="20" 
                              ShadowDepth="0" 
                              Opacity="0.3"/>
        </Border.Effect>

        <Grid Margin="30,20,30,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       x:Name="titluText"
                       Text="Adauga adresa noua"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#1A1A1A"
                       Margin="0,0,0,20"/>

            <TextBlock Grid.Row="1"
                       Text="Adresa:"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Foreground="#444444"
                       Margin="0,0,0,8"/>

            <TextBox Grid.Row="2"
                     x:Name="adresaTextBox"
                     FontSize="14"
                     Padding="10"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     BorderBrush="#F4B860"
                     BorderThickness="2"/>

            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right"
                        Margin="0,15,0,0">

                <Button Content="Anuleaza"
                        Width="120" Height="40"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Background="Transparent"
                        Foreground="#3A3A3A"
                        BorderBrush="#3A3A3A"
                        BorderThickness="1"
                        Cursor="Hand"
                        Margin="0,0,10,0"
                        Click="Anuleaza_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="20">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Content="Salveaza"
                        Width="120" Height="40"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Background="#F4B860"
                        Foreground="#3A3A3A"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="Salveaza_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="20">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E0A050"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

        </Grid>
    </Border>
</Window>